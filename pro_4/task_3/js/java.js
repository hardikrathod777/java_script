
function triangleArea(l, h) {
    let area = (l * h) / 2;
    return area;
  }
  
  let result = triangleArea(10, 8);
  document.getElementById('h2').innerHTML="The area of the triangle is :" + result; 